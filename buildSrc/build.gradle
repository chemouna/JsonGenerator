repositories {
  mavenCentral()
  maven {
    url "https://oss.sonatype.org/content/repositories/snapshots"
  }
}

rootProject.dependencies {
  compile project(':jsongenerator')
  compile project(':jsongenerator-plugin')
}
